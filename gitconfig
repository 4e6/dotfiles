[credential]
  helper = store
[core]
  editor = vim
  excludesfile = ~/.gitignore
[merge]
  tool = vimdiff
[push]
  default = simple
[alias]
  co = checkout
  ss = status
  glog = log --pretty=format:'%h : %s' --graph
  onelog = log --graph --oneline --decorate
  fetchlog = log ORIG_HEAD.. --stat --no-merges
[color]
  ui = auto
